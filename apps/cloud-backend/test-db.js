const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient({ datasources: { db: { url: 'postgresql://pharmaflow:sagar@localhost:5432/pharmaflow_prod' } } }); async function main() { console.log('Companies:', await prisma.company.findMany()); console.log('Users:', await prisma.user.findMany()); } main().catch(console.error).finally(() => prisma.$disconnect());
